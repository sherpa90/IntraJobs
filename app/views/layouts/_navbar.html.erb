<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>">IntraJobs</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="#">Logged in as: <%= current_user.email %></a>
          </li>
          <% if current_user.admin? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_registration_path %>">Sign Up</a>
          </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= new_job_offer_path %>">Crear</a>
            </li>
            <li class="nav-item">
            <%= link_to "Todas las Postulaciones", job_applications_path, class: "nav-link" %>
          </li>
          <% else %>
          <%= link_to "Mis Postulaciones", user_job_applications_path(current_user), class: "nav-link" %>
          
          <% end %>
          <li class="nav-item">
            <a class="nav-link" rel="nofollow" data-method="delete" href="<%= destroy_user_session_path %>">Logout</a>
          </li>
        <% else %>
          
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_session_path %>">Login</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
